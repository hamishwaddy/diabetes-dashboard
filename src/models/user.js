export class User {
  constructor (firstName, lastName, email, password, nightscoutUrl) {
    this.firstName = firstName,
    this.lastName = lastName,
    this.email = email,
    this.password = password,
    this.nightscoutUrl = nightscoutUrl
  }
}